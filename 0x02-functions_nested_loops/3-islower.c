#include "main.h"

/**
  * _islower -> checks if the character is lowercase
  * @c: is a character argument
  * Return: Returns 1 and 0depending on the condition
  */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
